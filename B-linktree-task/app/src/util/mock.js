const get = (provider, id) => {
    switch (key) {
        case value:
            return {test:1}
            
            break;
    
        default:
            break;
    }
}

module.exports = {
    get
}